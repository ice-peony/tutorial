#include <stdio.h>

int main()
{
    printf("Please input a nummber:\n");
    int a;
    scanf("%d",&a);

    int i = 1;
    while (a &= a-1)
    {
        i++;/* code */
    }
    
    printf("1 has : %d\n",i);
    return 0;

     // make by liupengbin
     // date is (rq)
    /*使用 snippet 快捷创建自定义的东西，很有用
        首先 ctrl+shift+p 搜索 configure user snippet
        选择你所创建 snippet 的范围
        再在里面根据注释提示写 $ 是变量到时候可以自定义 { ：} 可以创建默认参数*/ 
    
}
